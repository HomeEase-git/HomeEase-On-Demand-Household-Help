// Public copy of the Privacy Policy, so it can be read from sign-up before
// the user has an account (the profile copies are behind login).
export { default } from "../(client)/profile/privacy-policy";
